import React from "react";
import { render, screen } from "@testing-library/react";
import App from "./App";

test("Order App", () => {
  // render(<App />);
  // const linkElement = screen.getByText(/Order App/i);
  // expect(linkElement).toBeInTheDocument();
});
